# Repository-level Ansible defaults.
# CLI-generated and local test inventories should be passed explicitly with -i.

[defaults]
roles_path = roles
interpreter_python = auto_silent
retry_files_enabled = false

[ssh_connection]
pipelining = true
