# Appa Ansible

This directory contains Ansible playbooks and roles for host-level Appa server
hardening. See the centralized reference at [`docs/ansible.md`](../../docs/ansible.md)
for playbook docs, role descriptions, molecule testing, CI, and variable
reference.
